<%= form_for(@noun) do |f| %>
  <% if @noun.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@noun.errors.count, "error") %> prohibited this noun from being saved:</h2>

      <ul>
      <% @noun.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word %><br>
    <%= f.text_field :word %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
