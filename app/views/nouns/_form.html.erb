<%= form_for @noun, :url => nouns_path(@noun), :method => "post", :remote => true do |f| %>
  <% if @noun.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@noun.errors.count, "error") %> prohibited this noun from being saved:</h2>

      <ul>
      <% @noun.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="col-lg-6">
    <div class="input-group">
      <%= f.text_field :word, class: "form-control", remote: true %>
      <span class="input-group-btn">
        <button class="btn btn-primary addWordBtn" type="button" data-remote="true">Add!</button>
      </span>
    </div>
  </div>
</div>

<% end %>
