%ul.list-group.monsterList
	- @monsters.each do |m|
		%li.list-group-item
			= m.name
			-# = link_to m.like, like_monster_path(m), class: "btn btn-small pull-right" do
			= link_to likeMon_monster_path(:id => m), class: "btn btn-small pull-right likeBtn" do 
				%i.icon-thumbs-up-alt.pull-right
				- if m.likes_count == 1
					= "#{m.likes_count} Like"
				- else
					= "#{m.likes_count} Likes"
				
				-# = link_to m.like, like_monster_path(m), class: "btn btn-small pull-right" do
			-# %span.badge
			-# 	= m.like